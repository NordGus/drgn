<%= form_with(model: settings_invitation) do |form| %>
  <% if settings_invitation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(settings_invitation.errors.count, "error") %> prohibited this settings_invitation from being saved:</h2>

      <ul>
        <% settings_invitation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
