# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

DEFAULTS: &DEFAULTS
  type: "BossKey::Postmaster"

luffys_postmaster_key:
  <<: *DEFAULTS
  holder: luffy
  access_level: <%= BossKey::Postmaster.access_levels[:manage] %>

zoro_postmaster_key:
  <<: *DEFAULTS
  holder: zoro
  access_level: <%= BossKey::Postmaster.access_levels[:no] %>

nami_postmaster_key:
  <<: *DEFAULTS
  holder: nami
  access_level: <%= BossKey::Postmaster.access_levels[:manage] %>

kinemon_postmaster_key:
  <<: *DEFAULTS
  holder: kinemon
  access_level: <%= BossKey::Postmaster.access_levels[:no] %>

kanjuro_postmaster_key:
  <<: *DEFAULTS
  holder: kanjuro
  access_level: <%= BossKey::Postmaster.access_levels[:no] %>
  deleted_at: <%= 1.day.ago %>
